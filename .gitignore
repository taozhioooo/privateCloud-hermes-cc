# Secrets and local env
.env
.env.*
!.env.example
!.env.sample

# Runtime data
*.log
*.pid

# Python
__pycache__/
*.pyc

# Node
node_modules/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Rendered compose fragments (generated from users/users.yaml)
compose/rendered/*.yml
compose/rendered/*.yaml
!compose/rendered/.gitkeep

# Per-user env overrides
users/*/.env

# Upstream clones pulled on demand by CI (hermes-agent + hermes-web-ui)
images/hermes-agent/hermes-agent/
images/hermes-agent/hermes-web-ui/

# 旧镜像构建文件备份 - 本地留痕,不入库
images/hermes-agent.bak/

# Local demo runtime data (contains keys/secrets)
demo/
