# =============================================================================
# Hermes 集群基础层（新版，无 Redis）
# =============================================================================
# 只声明共享 Docker 网络。所有用户相关 service 由 users/users.yaml +
# compose/templates/user.yml.j2 动态生成到 compose/rendered/*.yml，
# 并通过 scripts/cluster 管理。
# =============================================================================

networks:
  hermes-net:
    driver: bridge
    name: hermes-net
